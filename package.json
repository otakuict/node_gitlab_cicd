{
  "name": "otakuict-api",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules  jest --coverage --testPathPattern=src/tests",
    "start": "node src/server.js",
    "build": "babel src -d dist",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^27.5.1",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4"
  },
  "type": "module"
}
